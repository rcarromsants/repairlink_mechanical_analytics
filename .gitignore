# Snowflake key files — NEVER commit these
my_snowflake_key.*
*.p8
*.priv

# dbt artifacts
logs/
target/
dbt_packages/

# dbt lock file (regenerated by `dbt deps`)
**/package-lock.yml

# OS / editor cruft
.DS_Store
*.swp
.vscode/
.idea/
